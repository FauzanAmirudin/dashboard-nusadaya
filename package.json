{
	"name": "dashboard-nusadaya",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"apps/*"
	],
	"scripts": {
		"dev": "bun --filter '*' --parallel dev",
		"build": "bun --filter web build",
		"start": "bun --filter '*' --parallel start",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"husky": "^9.1.7",
		"lint-staged": "^17.0.7",
		"mammoth": "^1.12.0",
		"prettier": "3.9.6"
	}
}
